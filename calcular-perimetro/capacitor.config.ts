import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'calcular.perimetro',
  appName: 'calcular-perimetro',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
